server:
  port: 80

spring:
  application:
    name: api-gateway

services:
  url:
    user_service: ${USER_SERVICE_URL:http://localhost:8080}
    auth_service: ${AUTH_SERVICE_URL:http://localhost:8081}
    order_service: ${ORDER_SERVICE_URL:http://localhost:8082}

security:
  jwt-key: ${JWT_KEY}