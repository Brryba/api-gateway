spring:
  application:
    name: api-gateway

security:
  jwt-key: ${JWT_KEY}

server:
  port: 80

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true